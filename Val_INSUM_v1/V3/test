# test_validacion.py
import duckdb
from repositorio_configuracion_simple import RepositorioConfiguracion
from config import DB_CONFIG

# Verificar qué validaciones hay para el insumo
with RepositorioConfiguracion(DB_CONFIG) as repo:
    validaciones = repo.obtener_validaciones(212)  # tu insumo
    print(f"Validaciones encontradas: {len(validaciones)}")
    for v in validaciones:
        print(f"\nID: {v['id_val']}")
        print(f"Tipo: {v['tipo_val']}")
        print(f"Query: {v['validacion']}")
        print(f"Descripción: {v['descripcion']}")
