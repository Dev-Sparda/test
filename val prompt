necesito una clase repositorio que cree una base de datos de datos, ya tengo una clse DBManager que teiene ya funciones base de manipulacion de base de datos (usa enter y exit, tiene un funcion ejecutar_dml(self, query params=None), cargar_df_tabla(self, df, nombre_tabla)) esta la recibe por constructor, esta clase repo debe recibir en el constructor la ruta hacia la base de datos en path, y el df de configuracion del insumo, estas configuraciones veven en varias tablas ya que se separa por parte del insumo, rango del insumo, definiciones del insumo, teniendo la logica de cada insumo puede tener una o mas partes (por ejemplo un excel que puede tener varias hojas), uno o mas rangos(por ejemplo dos tablas en la misma hoja de un excel), y la definicion de insumos donde cada registro es la configuracion de una columna como tal, ya tengo la query que baja esta info en un df ejemplo: 'ID_INSUMO': [1,1,1,1,1], 'ID_PARTE': [1,1,1,2,2], 'NOMBRE_PARTE': ['XXXX_MD', 'XXXX_MD', 'XXXX_MD', 'EURO pr', 'EURO pr'], 'NOMBRE_RANGO': ['PRECIOS', 'PRECIOS', 'PRECIOS', 'PRECIOS_EUR', 'PRECIOS_EUR'], 'FILA_INICIO': [3,3,3,3,3], 'COL_INICIO': [0,0,0,0,0], 'COL_FIN':[0,1,4,1,4], 'INDEX_COL': [0,1,4,1,4], 'NOMBRE_COLUMN': ['FECHA', 'TIPO_VALOR', 'PRECIO_LIMPIO', 'ISIN', 'MONEDA'], 'TIPO_DATO_GRAL':[DATE, STR, FLOAT, STR,STR], 'TIPO_DATO_SYS':[DATE, TEXT, REAL, TEXT,TEXT], KEY_COLUMN': [0,0,0,1,0], asi y pues obviamente entre mas partes, rangos y columnas va a crecer mas la cardinalidad, se configuro asi para desde la base manejar exceles con multiples partes como csv o text normales ya que todos tienen almenos una parte, un rango y n definiciones, la columna NOMBRE_PARTE es el nombre de la hoja para los exceles, para csv o txt pues es un campo irrelevante, la columna NOMBRE_RANGO es el nombre que tendra la tabla en la base para ese rango, fila inicio es para el skiprows de pandas, INDEX_COL es el indice de las columnas a usar del rango, COL_INICIO y COL_FIN delimita lo que abarca el rango, NOMBRE_COLUMN es el nombre que tendran las columnas en la base y en el df, key colum si es 1 es un pk si es 0 no, TIPO_DATO_GRAL es para hacer una validacion de tipado a nvel df, TIPO_DATO_SYS es para darle el tipo de dato a la tabla en el create if not exists, aqui a mi se me ocurre separ esto en 2 clase una clase Repo_crear_base y otra clase nomralizador_insumo que se encargue de leer el archivo y con esta config asi como la otra crea las tablas de la base este lee el exce, txt, csv y lo particiona en n dfs como lo hace al igual que las tablas de la base (el df se lee sin encabezados y me ponemos los NOMBRE_COLUMN) y ya haria la carga a la base, tambien en este punto de flujo tengo un variable con la ruta del archivo y su tipo en otra variable, viene como 'xlsx', 'csv', 'txt', 'html' este también puede recibir el df de config ya que irían en el mismo punto del flujo su llamada

#TODO describir generación de log de errores, tabla de control de versiones y validaciones anivel validador y base de insumo
